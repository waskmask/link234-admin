<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('partials/head', { title: 'Add profile · G1QR' }) %>
  </head>
  <body class="min-h-dvh text-gray-900">
    <div class="flex">
      <!-- Sidebar -->
      <%- include('partials/sidebar') %>

      <!-- Main column -->
      <div class="min-h-dvh flex-1 flex flex-col">
        <!-- Header -->
        <%- include('partials/appHeader') %>

        <!-- Page content -->
        <main class="flex-1 flex justify-center">
          <section class="w-full max-w-[930px] px-4 sm:px-6 py-6">
            <h1 class="text-2xl md:text-1xl font-bold -mt-6 max-[1206px]:-mt-4">
              Profile
            </h1>

            <!-- rest of page -->
          </section>
        </main>
      </div>
    </div>

    <!-- Overlay for mobile drawer -->
    <div id="appOverlay" class="fixed inset-0 bg-black/40 z-40 hidden"></div>
    <script defer src="/js/app-shell.js"></script>
  </body>
</html>
