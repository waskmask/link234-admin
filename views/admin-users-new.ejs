<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('partials/head', { title }) %>
  </head>
  <body class="min-h-screen flex flex-col bg-gray-50 text-gray-800">
    <%- include('partials/accHeader') %>

    <main class="flex-1 mx-auto w-full max-w-[900px] px-4 py-8">
      <div class="mb-6">
        <h1 class="text-xl font-semibold">Add Admin User</h1>
        <p class="text-sm text-gray-500 mt-1">
          Only Super Admin can create new admins.
        </p>
      </div>

      <div class="rounded-xl border border-gray-200 bg-white shadow-sm p-6">
        <!-- Success / Error alerts (hidden by default) -->
        <div
          id="alertSuccess"
          class="hidden mb-4 rounded-lg border border-emerald-200 bg-emerald-50 text-emerald-800 px-4 py-3 text-sm"
        >
          Admin created successfully.
        </div>
        <div
          id="alertError"
          class="hidden mb-4 rounded-lg border border-rose-200 bg-rose-50 text-rose-800 px-4 py-3 text-sm"
        >
          <!-- filled by JS -->
        </div>

        <form id="adminCreateForm" class="space-y-5" novalidate>
          <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
            <div>
              <label class="block text-sm text-gray-600 mb-1" for="name"
                >Full Name</label
              >
              <input
                id="name"
                name="name"
                type="text"
                autocomplete="name"
                class="block w-full rounded-lg border-gray-300 focus:border-blue-500 focus:ring-blue-500"
                placeholder="Jane Doe"
              />
              <p class="mt-1 text-xs text-rose-600 hidden" data-err="name"></p>
            </div>

            <div>
              <label class="block text-sm text-gray-600 mb-1" for="email"
                >Email</label
              >
              <input
                id="email"
                name="email"
                type="email"
                autocomplete="email"
                class="block w-full rounded-lg border-gray-300 focus:border-blue-500 focus:ring-blue-500"
                placeholder="admin@example.com"
              />
              <p class="mt-1 text-xs text-rose-600 hidden" data-err="email"></p>
            </div>
          </div>

          <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
            <div>
              <label class="block text-sm text-gray-600 mb-1" for="password"
                >Password</label
              >

              <div class="relative">
                <input
                  id="password"
                  name="password"
                  type="password"
                  autocomplete="new-password"
                  class="block w-full rounded-lg border-gray-300 focus:border-blue-500 focus:ring-blue-500 pr-10"
                  placeholder="••••••••"
                  inputmode="text"
                  autocapitalize="off"
                  spellcheck="false"
                />
                <!-- eye toggle -->
                <button
                  id="togglePwdBtn"
                  type="button"
                  class="absolute inset-y-0 right-0 px-3 flex items-center text-gray-500 hover:text-gray-800"
                  aria-label="Show password"
                  data-state="hidden"
                  tabindex="-1"
                >
                  <!-- simple two-state icon (CSS toggled via [data-state]) -->
                  <svg
                    class="h-5 w-5 pointer-events-none"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                  >
                    <g data-eye="visible">
                      <path
                        d="M1 12s4-7 11-7 11 7 11 7-4 7-11 7-11-7-11-7Z"
                      ></path>
                      <circle cx="12" cy="12" r="3"></circle>
                    </g>
                    <g data-eye="hidden" style="display: none">
                      <path d="M3 3l18 18"></path>
                      <path d="M10.58 10.58a2 2 0 102.83 2.83"></path>
                      <path
                        d="M9.88 5.09A10.39 10.39 0 0112 5c7 0 11 7 11 7a20.85 20.85 0 01-5.24 5.69"
                      ></path>
                      <path
                        d="M6.11 6.11A20.85 20.85 0 001 12s4 7 11 7a10.89 10.89 0 005.89-1.61"
                      ></path>
                    </g>
                  </svg>
                </button>
              </div>

              <p class="mt-1 text-xs text-gray-500">
                Min 8 chars, include a number & symbol. No spaces.
              </p>
              <p
                class="mt-1 text-xs text-rose-600 hidden"
                data-err="password"
              ></p>
            </div>

            <div>
              <label class="block text-sm text-gray-600 mb-1" for="role"
                >Role</label
              >
              <select
                id="role"
                name="role"
                class="block w-full rounded-lg border-gray-300 focus:border-blue-500 focus:ring-blue-500"
              >
                <option value="">Select a role</option>
                <option value="admin">Admin</option>
                <!-- Keep Super Admin available; you can hide it if you don't want delegation -->
                <option value="superadmin">Super Admin</option>
                <option value="moderator">Moderator</option>
                <option value="sales">Sales</option>
              </select>
              <p class="mt-1 text-xs text-rose-600 hidden" data-err="role"></p>
            </div>
          </div>

          <div class="pt-2">
            <button
              id="submitBtn"
              type="submit"
              class="inline-flex items-center gap-2 rounded-lg bg-blue-600 text-white px-4 py-2 hover:bg-blue-700 disabled:opacity-50 disabled:pointer-events-none"
            >
              <svg
                id="btnSpinner"
                class="hidden animate-spin h-4 w-4"
                viewBox="0 0 24 24"
              >
                <circle
                  class="opacity-25"
                  cx="12"
                  cy="12"
                  r="10"
                  stroke="currentColor"
                  stroke-width="4"
                ></circle>
                <path
                  class="opacity-75"
                  fill="currentColor"
                  d="M4 12a8 8 0 018-8v4a4 4 0 00-4 4H4z"
                ></path>
              </svg>
              <span>Create Admin</span>
            </button>
          </div>
        </form>
      </div>
    </main>

    <%- include('partials/footer') %>
    <script src="/public/js/axios.min.js"></script>
    <script src="/public/apicalls/admin-users.js"></script>
  </body>
</html>
