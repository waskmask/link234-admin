<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head', { title: (typeof title !== 'undefined' ?
    title : 'Something went wrong · G1QR') }) %>
  </head>
  <body class="min-h-dvh bg-gray-50 text-gray-900">
    <main class="min-h-dvh flex items-center justify-center px-4">
      <div class="w-full max-w-[600px]">
        <!-- Logo + slogan -->
        <div class="text-center mb-6">
          <img src="/images/logo.svg" alt="G1QR" class="mx-auto h-9" />
          <p class="mt-2 text-sm text-gray-600">Coz Every Second Matters.</p>
        </div>

        <!-- Card -->
        <div class="bg-white border border-gray-200 rounded-xl shadow-sm">
          <div class="p-6 text-center">
            <div
              class="mx-auto mb-3 size-12 rounded-full bg-red-100 flex items-center justify-center"
            >
              <!-- error icon -->
              <svg
                class="size-6 text-red-600"
                viewBox="0 0 20 20"
                fill="currentColor"
                aria-hidden="true"
              >
                <path
                  fill-rule="evenodd"
                  d="M10 18a8 8 0 1 0 0-16 8 8 0 0 0 0 16ZM9 6a1 1 0 0 1 2 0v5a1 1 0 1 1-2 0V6Zm1 9a1.25 1.25 0 1 1 0-2.5 1.25 1.25 0 0 1 0 2.5Z"
                  clip-rule="evenodd"
                />
              </svg>
            </div>
            <h1 class="text-2xl font-bold">Something went wrong</h1>
            <p class="mt-2 text-sm text-gray-600">
              We’re sorry—an unexpected error occurred. Please try again in a
              moment.
            </p>

            <% if (typeof incidentId !== 'undefined' && incidentId) { %>
            <p class="mt-2 text-xs text-gray-500">
              Incident ID: <%= incidentId %>
            </p>
            <% } %> <% if (typeof stack !== 'undefined' && stack) { %>
            <pre
              class="mt-4 text-left whitespace-pre-wrap text-xs bg-gray-50 border rounded-lg p-3 overflow-auto max-h-48"
            >
<%= stack %></pre
            >
            <% } %>

            <div class="mt-6 grid gap-2">
              <a
                href="/"
                class="max-w-[200px] mx-auto inline-flex w-full justify-center rounded-lg bg-blue-600 px-4 py-3 text-sm font-medium text-white hover:bg-blue-700"
              >
                Go to home
              </a>
              <a
                href="mailto:support@g1qr.com"
                class="max-w-[200px] mx-auto inline-flex w-full justify-center rounded-lg border px-4 py-3 text-sm font-medium hover:bg-gray-50"
              >
                Contact support
              </a>
            </div>
          </div>
        </div>
      </div>
    </main>

    <%- include('../partials/footer') %>
  </body>
</html>
