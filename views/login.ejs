<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('partials/head', { title: 'Admin Login · Link234' }) %>
  </head>
  <body class="min-h-dvh bg-gray-50 text-gray-900">
    <!-- Centered container -->
    <main class="min-h-dvh flex items-center justify-center px-4">
      <div class="w-full max-w-[400px]">
        <!-- Logo + slogan -->
        <div class="text-center mb-6">
          <img
            src="/public/images/l234-logo.svg"
            alt="G1QR"
            class="mx-auto h-9"
          />
          <p class="mt-2 text-sm text-gray-600">Admin login</p>
        </div>

        <!-- Card -->
        <div class="bg-white border border-gray-200 rounded-xl shadow-sm">
          <div class="p-5 sm:p-7">
            <div class="text-center">
              <h1 class="block text-2xl font-bold">Sign in</h1>
            </div>

            <div class="mt-5">
              <!-- Form -->
              <form>
                <div class="grid gap-y-4">
                  <!-- Email -->
                  <div>
                    <label for="email" class="block text-sm mb-2"
                      >Email address</label
                    >
                    <input
                      type="email"
                      id="email"
                      name="email"
                      class="py-3 px-4 block w-full border border-gray-300 rounded-lg sm:text-sm focus:border-blue-500 focus:ring-blue-500"
                      required
                    />
                  </div>

                  <!-- Password with show button -->
                  <div class="mb-3">
                    <div class="flex items-center justify-between gap-2">
                      <label for="password" class="block text-sm mb-2"
                        >Password</label
                      >
                      <a
                        class="text-sm text-blue-600 hover:underline font-medium"
                        href="/forgot-password"
                        >Forgot password?</a
                      >
                    </div>

                    <div class="relative">
                      <input
                        type="password"
                        id="password"
                        name="password"
                        class="py-3 px-4 pr-10 block w-full border border-gray-300 rounded-lg sm:text-sm focus:border-blue-500 focus:ring-blue-500"
                        required
                      />

                      <!-- Show password (auto-hide after 2s) -->
                      <button
                        type="button"
                        class="absolute inset-y-0 right-0 px-3 flex items-center text-gray-400 hover:text-gray-600"
                        aria-controls="password"
                        data-toggle-password
                        data-timeout="2000"
                        aria-label="Show password"
                      >
                        <!-- eye -->
                        <svg
                          class="eye-on size-5"
                          viewBox="0 0 20 20"
                          fill="currentColor"
                          aria-hidden="true"
                        >
                          <path
                            d="M10 4.5c-4.167 0-7.5 3.333-8.75 5.5 1.25 2.167 4.583 5.5 8.75 5.5s7.5-3.333 8.75-5.5C17.5 7.833 14.167 4.5 10 4.5Zm0 9a3.5 3.5 0 1 1 0-7 3.5 3.5 0 0 1 0 7Z"
                          />
                        </svg>
                        <!-- eye-off -->
                        <svg
                          class="eye-off size-5 hidden"
                          viewBox="0 0 20 20"
                          fill="currentColor"
                          aria-hidden="true"
                        >
                          <path
                            d="M3.28 2.22 2.22 3.28l2.01 2.01C2.9 7 1.7 8.6 1.25 10c1.25 2.167 4.583 5.5 8.75 5.5 1.67 0 3.21-.43 4.56-1.18l2.19 2.18 1.06-1.06L3.28 2.22ZM10 6.5c.37 0 .73.07 1.06.2l-1.36 1.36A1.5 1.5 0 0 0 8.5 9.5c0 .37.13.71.34.98l-1.1 1.1A3.5 3.5 0 0 1 10 6.5Z"
                          />
                        </svg>
                      </button>
                    </div>
                  </div>

                  <button
                    type="submit"
                    class="w-full py-3 px-4 inline-flex justify-center items-center gap-x-2 text-sm font-medium rounded-lg bg-blue-600 text-white hover:bg-blue-700"
                  >
                    Sign in
                  </button>
                </div>
              </form>
              <!-- End Form -->
            </div>
          </div>
        </div>

        <!-- Tip: make the border more visible by using bg-gray-50 on body (already set) and border-gray-200/300 -->
      </div>
    </main>

    <%- include('partials/footer') %>
    <script src="/public/js/axios.min.js"></script>
    <script src="/public/apicalls/login.js"></script>
  </body>
</html>
